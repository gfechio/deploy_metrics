log = dict(
        dir = "/var/log/deploy_manager/",
        file = "/var/log/deploy_manager/deploy_manager.log"
        )
db = dict(
        db = "deploy",
        user = "deploy",
        host = "localhost"
        )

